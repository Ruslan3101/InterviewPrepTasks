import React, { useState } from "react";

const Counter = () => {
  const style = {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  };

  return (
    <div style={style}>
      <h1>Counter</h1>
    </div>
  );
};

export default Counter;
